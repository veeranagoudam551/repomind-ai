# RepoMind AI — Architecture

This document is a living record of the system design. It grows as
each phase of the project is built; it is not written all at once.

## 1. High-Level System View

```
USER
  |
  v
NEXT.JS FRONTEND
  |
  v
FASTAPI BACKEND
  |
  |------------------------|
  |                        |
  v                        v
POSTGRESQL              REDIS
  |
  v
REPOSITORY METADATA
```

- **Next.js frontend** — the developer-facing UI (dashboard, repo view, chat).
- **FastAPI backend** — the single API surface for auth, repository
  ingestion, RAG, and analysis features.
- **PostgreSQL** — system of record for users, repositories, files,
  chunk metadata, conversations, and messages.
- **Redis** — backing store for background job queues (repository
  processing runs async, not inline with the HTTP request).
- **Qdrant** (introduced Phase 3) — vector store for code embeddings,
  used for semantic search and RAG retrieval.

## 2. Repository Processing Pipeline (Phase 2)

```
GitHub Repository
        |
        v
Repository Validation
        |
        v
Repository Clone / Download
        |
        v
File Scanner
        |
        v
File Parser
        |
        v
Code Chunking
        |
        v
Embedding Generation
        |
        v
QDRANT VECTOR DATABASE
```

Repository code is only ever read, never executed. See Security
Requirements below.

## 3. RAG Pipeline (Phase 3)

```
User Question
        |
        v
Question Embedding
        |
        v
Qdrant Semantic Search (filtered by repository_id)
        |
        v
Retrieve Relevant Code Chunks
        |
        v
Build Context
        |
        v
LLM
        |
        v
Grounded Answer + Source Files
```

The first three steps (embed the question, search Qdrant, retrieve the
matching chunks) were implemented standalone in Day 16 via
`POST /repositories/{id}/search`. The full pipeline, including
conversation/message persistence, is wired up as of Day 18 via
`POST /conversations/{id}/messages`, using Day 17's Anthropic client
for the "LLM" step.

The LLM is instructed to answer only from retrieved context, state
uncertainty explicitly, and never claim a file or function exists
that was not actually retrieved.

## 4. Data Model

All tables use a `UUID` primary key (`id`). Implemented as async
SQLAlchemy 2.0 models in `backend/app/models/`, with an Alembic
migration (`backend/alembic/versions/`) as the source of truth for the
actual schema.

**`users`** — account credentials and profile
| Column | Type | Notes |
|---|---|---|
| `email` | string(320) | unique, indexed |
| `hashed_password` | string(255) | never store plaintext |
| `full_name` | string(255) | nullable |
| `is_active` | bool | default `true` |
| `created_at` / `updated_at` | timestamptz | |

**`repositories`** — a GitHub repo a user has added, plus processing status/stats
| Column | Type | Notes |
|---|---|---|
| `owner_id` | UUID FK → `users.id` | `ON DELETE CASCADE`, indexed |
| `github_url` | string(2048) | |
| `name` | string(255) | e.g. `owner/repo` |
| `description` | text | nullable |
| `default_branch` | string(255) | nullable |
| `status` | enum: `pending`/`cloning`/`processing`/`completed`/`failed` | default `pending` |
| `error_message` | text | nullable |
| `file_count` | int | default `0` |
| `total_size_bytes` | bigint | default `0` |
| `last_indexed_at` | timestamptz | nullable |
| `created_at` / `updated_at` | timestamptz | |

Unique constraint on `(owner_id, github_url)` — a user can't add the
same repo twice.

**`conversations`** — a chat thread scoped to one repository
| Column | Type | Notes |
|---|---|---|
| `repository_id` | UUID FK → `repositories.id` | `ON DELETE CASCADE`, indexed |
| `user_id` | UUID FK → `users.id` | `ON DELETE CASCADE`, indexed |
| `title` | string(255) | nullable |
| `created_at` / `updated_at` | timestamptz | |

**`messages`** — individual turns within a conversation
| Column | Type | Notes |
|---|---|---|
| `conversation_id` | UUID FK → `conversations.id` | `ON DELETE CASCADE`, indexed |
| `role` | enum: `user`/`assistant`/`system` | |
| `content` | text | |
| `source_chunk_ids` | JSONB, nullable | Day 18; `code_chunks.id` values retrieved to ground an assistant reply, in relevance order |
| `created_at` | timestamptz | |

**`repository_files`** — per-file metadata for a processed repository
| Column | Type | Notes |
|---|---|---|
| `repository_id` | UUID FK → `repositories.id` | `ON DELETE CASCADE`, indexed |
| `file_path` | string(1024) | relative path within the repo |
| `language` | string(64) | nullable |
| `size_bytes` | int | default `0` |
| `content_hash` | string(64) | nullable, for change detection |
| `created_at` | timestamptz | |

Unique constraint on `(repository_id, file_path)`.

**`code_chunks`** — metadata for each chunk sent to the vector DB
(vectors themselves live in Qdrant, not Postgres)
| Column | Type | Notes |
|---|---|---|
| `repository_id` | UUID FK → `repositories.id` | `ON DELETE CASCADE`, indexed (denormalized for fast filtering) |
| `repository_file_id` | UUID FK → `repository_files.id` | `ON DELETE CASCADE`, indexed |
| `chunk_index` | int | order within the file |
| `content` | text | |
| `start_line` / `end_line` | int | nullable |
| `vector_id` | string(64) | indexed; the corresponding point ID in Qdrant |
| `created_at` | timestamptz | |

## 5. Security Principles (apply from Day 1 onward)

- Repository code is **read-only** — the system never executes code
  from an ingested repository (no `npm install`, no running scripts).
- Secrets (JWT key, DB credentials, API keys) live only in `.env`,
  never in source. See `.env.example` for the full contract.
- Repository access is always scoped to the owning user — no
  cross-user data leakage, including in vector search filters.
- Dangerous/irrelevant directories (`.git`, `node_modules`, `.venv`,
  build output, etc.) are excluded from ingestion.

## 6. Status Log

| Day | Milestone |
|---|---|
| Day 1 | Project structure, git repo, env contract, this doc |
| Day 2 | Next.js frontend scaffold (TypeScript, Tailwind, shadcn/ui, TanStack Query); base layout with nav/footer shell; placeholder landing, login, and dashboard pages |
| Day 3 | FastAPI backend skeleton (`app/main.py`, `app/core/config.py` via pydantic-settings, `app/api/health.py`); `/health` endpoint; CORS configured for the frontend origin |
| Day 4 | Async SQLAlchemy 2.0 models for all 6 tables (`app/models/`); async engine/session setup (`app/core/database.py`); Alembic configured for async migrations; initial migration generated and applied against a live local PostgreSQL 17 database |
| Day 5 | Auth: password hashing (bcrypt) and JWT issuing/verification (`app/core/security.py`); `POST /auth/register`, `POST /auth/login`, `GET /auth/me` (protected via `app/api/deps.py`); full flow verified end-to-end against the live database |
| Day 6 | GitHub API client (`app/services/github.py`) — parses a repo URL, fetches metadata via the GitHub REST API; `POST /repositories` (validates the repo, enforces `MAX_REPO_SIZE_MB`, rejects private repos and duplicates, creates a `pending` row) and `GET /repositories` (list your own), both protected; verified against real public GitHub repos |
| Day 7 | Repository download + file scanner (`app/services/repository_ingestion.py`) — downloads the default branch as a tarball via the GitHub API (no `git` subprocess), safely extracts it, walks the tree excluding `.git`/`node_modules`/`.venv`/build output/etc., skips files over `MAX_FILE_SIZE_KB`, hashes and language-tags the rest, and writes `repository_files` rows; runs via FastAPI `BackgroundTasks` (Celery/Redis lands Day 34) and drives `repositories.status` through `pending → cloning → processing → completed`/`failed`; added `GET /repositories/{id}` and `GET /repositories/{id}/files`; verified end-to-end against real public repos (`octocat/Hello-World`, `github/gitignore`) |
| Day 8 | File parser + code chunking (`app/services/code_chunking.py`) — for each scanned file, skips binary content (by extension and a null-byte sniff), decodes the rest as UTF-8, and splits it into overlapping line-window chunks (`CHUNK_MAX_LINES`, default 100; `CHUNK_OVERLAP_LINES`, default 15), preserving `start_line`/`end_line`; wired into `repository_ingestion.py` right after each `repository_files` row is written, populating `code_chunks` (`vector_id` stays `null` until Qdrant on Day 15); added `GET /repositories/{id}/chunks` (optional `file_id` filter); verified against real repos — a 705-line file produced 9 correctly-overlapping chunks, `github/gitignore`'s 319 files produced 345 chunks |
| Day 9 | Automated test suite (`backend/tests/`, pytest + pytest-asyncio) covering auth (register/login/me, including duplicate emails, wrong passwords, bad tokens) and repositories (create/list/get, ownership isolation, validation errors, private/oversized rejection, duplicates, empty files/chunks before ingestion) — replaces the manual curl verification used through Day 8; runs against a real Postgres database (`<db>_test`, auto-created, schema recreated per test) with GitHub API calls and the background ingestion task stubbed out so it's fast, deterministic, and never touches the dev database; 21 tests, all passing; sanity-checked by deliberately breaking a status code and confirming the suite catches it |
| Day 10 | Repository management — `DELETE /repositories/{id}` (removes the row; `repository_files`/`code_chunks`/`conversations` cascade via existing FK `ondelete="CASCADE"`) and `POST /repositories/{id}/reindex` (re-runs the Day 7-8 ingestion pipeline for an existing repository); reindex claims the job with a single conditional `UPDATE ... WHERE status NOT IN (pending, cloning, processing)` so two concurrent reindex calls can't both start an ingestion for the same repo — one succeeds, the other gets `409 Conflict`; 7 new tests (28 total) covering delete + cascade, ownership checks, the in-progress guard, and that reindex actually re-queues the background task; verified live against the real database (create → delete → cascade confirmed via `GET .../files`) |
| Day 11 | Frontend wired to the real backend — login/register are now real forms (`app/actions/auth.ts`, React Server Actions + `useActionState`) that call the Day 5 auth endpoints and store the returned JWT in an httpOnly cookie (`lib/session.ts`; expiry taken straight from the token's own `exp` claim); the dashboard (`app/dashboard/page.tsx`) is an async Server Component that calls `GET /auth/me` and `GET /repositories` directly (server-to-server, so no CORS involved) and renders real status badges instead of placeholder data, plus an add-repository form and per-repo delete wired to `POST/DELETE /repositories`; route protection lives in `src/proxy.ts` — this Next.js version (16) renamed `middleware.ts` to `proxy.ts` — doing an optimistic cookie-presence check to redirect `/dashboard` when logged out and `/login`+`/register` when already logged in; the header now shows Log out vs. Log in/Get started based on the same cookie, read in the (now async) root layout. `tsc --noEmit` and `eslint` both pass; verified live with curl against real dev servers — proxy redirects both directions, and the dashboard genuinely renders "Signed in as `<email>`" by hitting the real API with a real JWT. The interactive form submissions (actual browser clicks through login/register/add-repository) were not exercised in a real browser since no browser-automation tool is available in this environment — worth a manual click-through. |
| Day 12 | Repository detail page (`app/dashboard/[id]/page.tsx`) — clicking a repo on the dashboard now goes to a page showing full status, the failure `error_message` when one exists, default branch/size/last-updated, a Reindex button wired to Day 10's `POST /repositories/{id}/reindex` (disabled client-side while ingestion is already in progress, and the action swallows a `409` gracefully since the page revalidates either way), a Delete button that redirects back to `/dashboard`, and a scrollable table of every scanned file from `GET /repositories/{id}/files`; unknown/not-owned IDs render a custom `not-found.tsx` (backend 404 mapped via Next's `notFound()`) instead of the generic framework page. `formatBytes` moved to `lib/utils.ts` for reuse. `tsc --noEmit` and `eslint` pass clean; verified live end-to-end — real ingested repo's detail page renders its actual file (`README`), and an unknown ID correctly 404s with the site's own header/footer intact. Interactive clicks (Reindex/Delete buttons) weren't exercised in a real browser, same disclosed gap as Day 11. |
| Day 13 | Playwright end-to-end tests (`frontend/tests-e2e/`) — closes the Day 11-12 gap: a real Chromium browser now drives the actual UI instead of curl-checking SSR output. `global-setup.ts` starts the real FastAPI backend (or reuses one already running) and tears it down after; `playwright.config.ts` starts the Next.js dev server itself. 7 tests across `auth.spec.ts` (register → dashboard → log out → log back in, wrong-password error, unauthenticated redirect, authenticated-visitor redirected away from `/login`) and `repository.spec.ts` (add a real repo, wait for background ingestion to complete by polling via reload, view its scanned file, reindex, delete, invalid-URL validation error, unknown-ID not-found page) — exercising real ingestion end-to-end through the actual browser UI, not mocks. Caught two real test bugs in the first run (a race between clicking Log out and asserting the redirect landed, and `CardTitle` rendering a `<div>` rather than a semantic heading so `getByRole("heading")` never matched) — both fixed; all 7 pass. Test users are cleaned from the dev database after each run, same as every other day's manual verification. |
| Day 14 | Embedding generation (`app/services/embeddings.py`) — `generate_embedding`/`generate_embeddings` call OpenAI's `/v1/embeddings` (model from `EMBEDDING_MODEL`, default `text-embedding-3-small`), batching in groups of 100 and re-sorting each response by `index` so vectors can't silently misalign with their input texts on a reordered response. Deliberately a standalone, OpenAI-specific module rather than part of a shared "LLM provider" abstraction — Anthropic has no embeddings endpoint, so the eventual chat/RAG provider (Phase 3) and this will always be different providers regardless of how that's built. Not yet wired into the ingestion pipeline or persisted anywhere: `code_chunks.vector_id` points into Qdrant, which doesn't exist until Day 15, so wiring the two together happens then. No `OPENAI_API_KEY` was available in this environment, so verification is 6 unit tests mocking the OpenAI call via `httpx.MockTransport` (success, order-preservation on a reordered response, batch-splitting at the 100-item boundary, missing-key config error, API error, empty-input short-circuit that asserts no HTTP call is made) — live verification against the real API is a disclosed gap, same shape as the browser-testing gap on Days 11-12, until a key is added to `.env`. 34/34 backend tests pass. |
| Day 15 | Qdrant wired in (`app/services/vector_store.py`) — a thin `httpx` REST wrapper over Qdrant (`ensure_collection`, `upsert_chunks`, `delete_by_repository`), matching the plain-HTTP style of `github.py`/`embeddings.py` rather than adding the `qdrant-client` SDK dependency for three calls. Point IDs are the `code_chunks.id` UUID itself, so `code_chunks.vector_id` (schema since Day 8) is always just `str(chunk.id)` once embedded — no separate ID scheme to reconcile between Postgres and Qdrant. Wired into `repository_ingestion.py`: after all files for a repo are scanned and chunked, every chunk's content is embedded in one batch (Day 14's `generate_embeddings`) and upserted into Qdrant with `repository_id`/`code_chunk_id`/`repository_file_id`/line-range payload (payload carries enough to filter and locate a hit; full chunk content is still fetched from Postgres, not duplicated into Qdrant); any prior vectors for the repository are deleted first so a reindex's fresh chunk UUIDs don't leave orphaned points behind from the previous run. An embedding or Qdrant failure fails the whole ingestion run (`status` → `failed`, real error message), same as any other ingestion step. `DELETE /repositories/{id}` now also deletes the repository's vectors from Qdrant (before the Postgres row, so a Qdrant failure leaves the repository intact and retryable rather than orphaning vectors with no `code_chunks` row left pointing at them). New config: `QDRANT_HOST`/`QDRANT_PORT`/`QDRANT_COLLECTION_NAME` (already documented in `.env.example` since Day 1). Automated verification: 10 unit tests for `vector_store.py` mocking Qdrant's HTTP responses via `httpx.MockTransport` (collection-exists/missing, create failure, upsert empty-short-circuit and payload shape, delete-by-filter payload and 404-as-noop, API-error handling for each), plus 3 tests driving `ingest_repository` directly end-to-end against the test database with an in-memory tarball standing in for the GitHub download and embeddings/Qdrant mocked — proving chunks actually get `vector_id` set, the right payload reaches the (mocked) upsert call, ingestion with zero chunks skips embedding entirely, and an embedding failure fails the repository with the real error message. No Docker is installed on this machine, so a real Qdrant server was obtained a different way: downloaded Qdrant's standalone Windows binary directly from its GitHub release and ran it locally on `localhost:6333` (had to relocate it out of a deeply-nested temp path first — Windows' `MAX_PATH` limit was breaking Qdrant's on-disk storage) — `ensure_collection`/`upsert_chunks`/`delete_by_repository` were then verified against that real instance (create collection, upsert two points across two repositories, delete one repository's points by filter, confirm via a raw scroll query that only the other repository's point survived). Real OpenAI embeddings remain the one disclosed gap, same shape as Day 14's, pending an `OPENAI_API_KEY`. 48/48 backend tests pass. |
| Day 16 | Semantic code search — `POST /repositories/{id}/search` (`{"query": str, "limit": int}` → ranked code chunks), the first three steps of the Phase 3 RAG pipeline (embed the question, search Qdrant filtered by `repository_id`, retrieve the matching chunks) as a standalone endpoint, ahead of "Build Context" → LLM which need the still-unbuilt LLM provider abstraction and conversation/message persistence. `vector_store.py` gained a `search()` call (`POST /collections/{name}/points/search` with a `repository_id` payload filter, `with_payload: true`); the endpoint embeds the query (Day 14's `generate_embedding`), searches Qdrant, then joins the hits' `code_chunk_id`s back against Postgres for `content`/`file_path`/line range, re-sorting by Qdrant's score since a Postgres `IN` fetch doesn't preserve order. `EmbeddingConfigError`/`EmbeddingAPIError` map to `503`/`502`; a `VectorStoreError` from Qdrant also maps to `502`; no results (never-ingested repo, or a query with no close match) is a `200` with an empty list, not an error. 9 new tests (3 for `vector_store.search` mocking Qdrant's HTTP response, 6 for the endpoint covering ranking/ownership/empty-results/config-error/validation) — 57/57 backend tests pass. Verified live against a real Qdrant instance (upserted vectors across two repositories with a distinctive one closest to the query vector, confirmed `search()` returns only the matching repository's points in score order) using the same locally-run Qdrant binary from Day 15; the query-embedding half of the endpoint still depends on the same disclosed `OPENAI_API_KEY` gap as Days 14-15. |
| Day 17 | LLM provider abstraction (`app/services/llm.py`) — `generate_response(system_prompt, user_message, max_tokens)` calls Anthropic's `/v1/messages` (model from `ANTHROPIC_MODEL`, default `claude-sonnet-5`), same thin-`httpx`-wrapper style as `embeddings.py`/`github.py`/`vector_store.py` rather than the `anthropic` SDK. A separate module from embeddings by design (same reasoning as Day 14's entry): Anthropic has no embeddings endpoint, so embeddings (OpenAI) and chat (Anthropic) are always two different providers regardless of `LLM_PROVIDER`; only Anthropic is implemented today, with a provider branch anticipated inside this same function signature if a second one is ever added rather than a premature interface now. New config: `LLM_PROVIDER` (already in `.env.example` since Day 1), `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`. Stateless — no notion of prior turns; conversation history is the caller's responsibility (Day 18). No `ANTHROPIC_API_KEY` was available in this environment, so verification is 5 unit tests mocking the Anthropic call via `httpx.MockTransport` (success, multi-block text concatenation, missing-key config error, API error, model/max_tokens passed through correctly) — live verification is a disclosed gap, same shape as Day 14's for OpenAI. 62/62 backend tests pass. |
| Day 18 | The RAG pipeline completed — `app/api/conversations.py`: `POST`/`GET /repositories/{id}/conversations` and `POST`/`GET /conversations/{id}/messages`. Sending a message stores the user's turn, embeds it (Day 14), searches Qdrant filtered by the conversation's repository (Day 16), and — if anything was retrieved — builds a context block from the matching `code_chunks` (file path, line range, content) and calls Day 17's `generate_response` with a system prompt that instructs the model to answer only from that context and say so explicitly rather than guessing when it can't. A never-ingested repository or a question with no close match short-circuits to a canned "couldn't find any indexed code" reply without calling the LLM at all — cheaper and matches the architecture principle of never claiming a file/function exists that wasn't actually retrieved. The assistant's reply is persisted with `source_chunk_ids` (new nullable `JSONB` column on `messages`, migration `1e10fd8521a7`) so `GET .../messages` can resolve and return the grounded `content` plus its `sources` (file path, line range) for every past turn, not just the live response. Ownership is enforced through the conversation's `user_id`, one hop removed from the repository check every other endpoint uses. `EmbeddingConfigError`/`LLMConfigError` map to `503`, `EmbeddingAPIError`/`LLMAPIError`/`VectorStoreError` to `502`. 15 new tests (5 for `llm.py` in Day 17, 10 for the conversations router covering the full mocked RAG round-trip, ownership isolation, the no-context short-circuit, and both config-error mappings) — 72/72 backend tests pass. Verified live: the `source_chunk_ids` migration was generated with `alembic revision --autogenerate` and applied against the real dev database; the new routes were confirmed registered via the running app's `/openapi.json`. Both the embedding and the LLM call remain disclosed gaps pending real `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` values, same shape as every AI-provider day since Day 14. |
| Day 19 | Chat UI — closes the Days 16-18 gap between the RAG backend and the frontend, same shape as Days 11-13's ingestion-UI work. New `/dashboard/[id]/chat` (lists a repository's conversations, "New chat" button) and `/dashboard/[id]/chat/[conversationId]` (message thread + input) pages, plus a "Chat" button on the repository detail page (enabled only once `status` is `completed`). Sending a message is a Server Action (`sendMessageAction`) calling Day 18's `POST /conversations/{id}/messages`, following the same no-client-fetch pattern as every other mutation in the app (`lib/api.ts` is server-only). Each message renders as a bubble (`Avatar` from the shadcn set, unused until now) with the assistant's `sources` shown as file-path chips below its reply. Verified live in a real Chromium browser via Playwright (no `chromium-cli` in this environment): register → add a repo → since ingestion fails without `OPENAI_API_KEY` (the disclosed gap since Day 15), the one test repository's `status` was flipped to `completed` directly in the dev database to unblock UI verification independent of that gap → Chat button → New chat → empty thread → send a message. Caught and fixed a real bug this way: `sendMessageAction`'s `catch` block returned early on a failed send, skipping the `revalidatePath` call below it — since the backend persists the user's turn *before* attempting the embedding call that then fails, the UI was silently hiding an already-saved message instead of showing it. Fixed by always revalidating regardless of outcome; re-verified the user's question now appears in the thread alongside the inline `OPENAI_API_KEY is not configured` error. `tsc --noEmit` and `eslint` both pass (after running `next typegen` to pick up the new typed routes). Test user/repository cleaned from the dev database afterward. The full RAG round-trip (a real grounded answer, not just the graceful-failure path) remains unverified pending real `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` values. |
| Day 20 | Fixed a real, silent regression in the Day 13 Playwright suite: `repository.spec.ts` asserted ingestion always reaches `"completed"`, but Day 15 made that require a real `OPENAI_API_KEY` — without one, ingestion now legitimately ends in `"failed"`, and the assertion had been timing out (20s) unnoticed through Days 15-19 since nothing re-ran the e2e suite in between. Found while investigating whether this was true, then hit a second, unrelated real issue trying to confirm it: the unauthenticated GitHub API rate limit (60/hr, no `GITHUB_TOKEN` in `.env`) was fully exhausted from the repeated live verification across Days 15-19, so `POST /repositories` failed outright before ingestion even started. The user added a real `GITHUB_TOKEN` to `.env` (confirmed via the GitHub `/rate_limit` endpoint: 5000/5000 available afterward), which unblocked testing the actual hypothesis. Fixed the test to wait for *either* terminal status and assert the right thing for whichever one actually happens (`README` visible on `completed`, the real `OPENAI_API_KEY is not configured` error text on `failed`) rather than assuming a single outcome — so the suite tells the truth about the environment it's running in instead of silently timing out, and will automatically exercise the full real path once a real `OPENAI_API_KEY` exists. Re-ran the full suite (all 7 tests, `auth.spec.ts` + `repository.spec.ts`) end-to-end with the new token: all pass, all test users cleaned up automatically. No application code changed — this was a test-suite-only fix. |
| Day 21 | Semantic code search UI — closes the same kind of gap Day 19 closed for chat, but for Day 16's `POST /repositories/{id}/search`, which had no frontend at all until now. New `/dashboard/[id]/search` page and a "Search" button on the repository detail page (same `status === "completed"` gating as Chat). Deliberately a plain `<form>` with no `action`/Server Action — a search is a query, not a mutation, so a native GET form submission that round-trips through the URL's `?q=` search param (read via `searchParams` in the Server Component) gives a shareable, bookmarkable, back-button-friendly page for free, unlike the rest of the app's Server-Action-driven mutations. Each result renders file path, line range, a similarity percentage, and the chunk's content in a `<pre>` block. Verified live in a real Chromium browser via Playwright, same approach as Day 19: register → add a repo (now via the real GitHub API with no rate-limit trouble, thanks to Day 20's `GITHUB_TOKEN`) → since ingestion still fails without `OPENAI_API_KEY`, the test repository's `status` was flipped to `completed` directly in the dev database to test the UI independent of that gap → clicked Search → submitted a query → got the graceful inline `OPENAI_API_KEY is not configured` error, no crash, no console errors, and the query stayed in the input (a natural side effect of driving the value from the URL rather than component state, so nothing needed fixing here the way Day 19's send-message form did). Also confirmed the shared ownership/404 handling works unmodified for the new route. `tsc --noEmit` and `eslint` pass (`next typegen` re-run for the new route). Backend untouched — still 72/72. Test user/repository cleaned up afterward. A real ranked result list (not just the graceful-failure path) remains unverified pending a real `OPENAI_API_KEY`. |
| Day 22 | File explanation — `POST /repositories/{id}/files/{file_id}/explain`, the first of the "Code review and code explanation" planned features. Deliberately independent of embeddings/Qdrant: the target file is already known, so there's nothing to search for — it reconstructs the file's original content directly from its `code_chunks` and asks the LLM (Day 17) to explain it, making this the first AI feature that depends on `ANTHROPIC_API_KEY` alone rather than both provider keys. New `reconstruct_file_content()` in `code_chunking.py` undoes the Day 8 chunker's overlapping windows — naively concatenating `chunk.content` values would repeat `CHUNK_OVERLAP_LINES` lines at every boundary, so it walks chunks in `chunk_index` order and, using each one's own `start_line`/`end_line`, keeps only the lines not already covered by a previous chunk. Verified with a round-trip test: real text through the real `chunk_text()` chunker, then back through `reconstruct_file_content()`, byte-for-byte equal to the original — including a shuffled-order case, since the function sorts defensively rather than trusting caller order. A file with no chunks (binary, or ingestion incomplete) returns `400`; `LLMConfigError`/`LLMAPIError` map to `503`/`502`, matching every other LLM-backed endpoint. 10 new tests (5 for the reconstruction round-trip, 5 for the endpoint: success, unknown file, cross-user ownership, no-chunks rejection, config-error mapping) — 82/82 backend tests pass. Verified live against the real running app and real database (not just pytest's client): registered a user, created a real repository via the (now-unrate-limited, thanks to Day 20) GitHub API, inserted a real file+chunk directly to bypass the still-open embedding-provider gap, then called the endpoint over HTTP — got the expected `503 ANTHROPIC_API_KEY is not configured`, confirming the full path (auth, ownership, DB fetch, reconstruction, LLM call attempt) actually executes correctly end-to-end; also confirmed the `404` path and the route's registration via `/openapi.json`. No frontend yet — same backend-then-UI rhythm as Day 16 (search) → Day 19/21; the UI for this lands next. A real explanation from Claude remains unverified pending a real `ANTHROPIC_API_KEY`. |
| Day 23 | File explanation UI — closes the gap Day 22 left open, same shape as Day 19 (chat) and Day 21 (search). New `/dashboard/[id]/files/[fileId]/explain` page and an "Explain" link on every row of the repository detail page's file table (no `status === "completed"` gating, since explanation only needs a file's `code_chunks`, which exist independent of embeddings/Qdrant per Day 22's design). The page fetches the file list (`GET /repositories/{id}/files`) to always show the file's path — including on failure, since a `503`/`502` response body carries only `detail`, not `file_path` — then separately calls Day 22's `explain` endpoint and renders either the explanation or the inline error, never a crash. `explainRepositoryFile()` added to `lib/api.ts`, `next typegen` re-run for the new route, `tsc --noEmit` and `eslint` both pass. Verified live in a real Chromium browser via a throwaway Playwright script (same technique as Days 19/21): registered a user, created a real repository via the live GitHub API, inserted a real file+chunk directly into Postgres to bypass the still-open `OPENAI_API_KEY` ingestion gap, logged in through the actual UI, clicked "Explain" from the file table, and confirmed the target page renders the file path and the graceful inline `ANTHROPIC_API_KEY is not configured` error — no console errors, no crash. Caught and fixed a real bug this way: the page originally only had `file_path` from a *successful* explain response, so the error state rendered with no path at all; fixed by sourcing the path from the files list instead, independent of whether the explain call itself succeeds. Backend untouched — still 87/87 (see Day 24). A real rendered explanation (not just the graceful-failure path) remains unverified pending a real `ANTHROPIC_API_KEY`. |
| Day 24 | Code review — `POST /repositories/{id}/files/{file_id}/review`, the second of the "Code review and code explanation" planned features named in Day 22. Same shape as `explain` end-to-end: reconstructs the file via Day 22's `reconstruct_file_content()`, calls the Day 17 LLM provider with a review-specific system prompt (real bugs/security issues/edge cases/code smells with concrete suggestions, explicitly told not to manufacture nitpicks on clean code), and maps `LLMConfigError`/`LLMAPIError` to `503`/`502` like every other LLM-backed endpoint. The file/chunk-fetch/404/400 logic shared with `explain` was pulled into a private `_get_file_content_for_llm()` helper rather than duplicated a second time. New `ReviewFileResponse` schema (`file_path`, `review`). 5 new tests mirroring Day 22's exactly (success, unknown file, cross-user ownership, no-chunks rejection, config-error mapping) — 87/87 backend tests pass. Verified live against the real running app and real database, same technique as Day 22: real user, real GitHub-backed repository, a directly-inserted real file+chunk, called over HTTP — got the expected `503 ANTHROPIC_API_KEY is not configured` for both `/review` and a `404` for an unknown file, and confirmed both routes are registered via `/openapi.json`. No frontend yet, same backend-then-UI rhythm as Day 22 → 23. A real review from Claude remains unverified pending a real `ANTHROPIC_API_KEY` — per explicit instruction, this stays a documented gap rather than something worked around with a fake key. |
| Day 25 | Code review UI — closes the gap Day 24 left open, identical shape to Day 23's explain UI. New `/dashboard/[id]/files/[fileId]/review` page (fetches the files list for the path so it renders even on a failed call, same fix Day 23 made; calls Day 24's `review` endpoint; shows the review text or a graceful inline error) and a "Review" link added next to the existing "Explain" link in the file table's actions column, both now sitting in a shared flex row. `reviewRepositoryFile()` added to `lib/api.ts`, `next typegen` re-run, `tsc --noEmit` and `eslint` both pass. Verified live in a real Chromium browser via a throwaway Playwright script, same technique as Day 23: a fresh `e2e_`-prefixed user (auto-cleaned by the e2e suite's existing teardown, rather than a manual cleanup script), a real GitHub-backed repository, a directly-inserted real file+chunk — logged in through the actual UI, clicked "Review" from the file table, landed on the right page, saw the file path and the graceful inline `ANTHROPIC_API_KEY is not configured` error, then confirmed the pre-existing "Explain" link still works unaffected alongside it. Backend untouched — still 87/87. A real rendered review (not just the graceful-failure path) remains unverified pending a real `ANTHROPIC_API_KEY`. |
| Day 26 | AI-assisted debugging — `POST /repositories/{id}/debug`, the next unstarted item on the README's planned-features list after chat, search, and code review/explanation. Unlike explain/review (which target one already-known file), a bug description doesn't name a file, so this reuses the full Day 16/18 RAG pipeline instead: embed the description (Day 14), semantic-search Qdrant across the whole repository (Day 16) rather than looking at a single file's chunks, then call the LLM (Day 17) with a debugging-specific system prompt (identify the likely root cause and a concrete fix, cite specific files/functions from the retrieved context, and explicitly say so rather than guess when the context is insufficient). Deliberately a standalone one-shot endpoint with no persisted conversation, matching the shape Day 16's search took ahead of Day 18's full chat feature — `DebugRequest`/`DebugResponse` are new schemas, the response's `sources` reuses `CodeSearchResult` (score included, same as `/search`) so a future UI can show exactly which snippets grounded the diagnosis. A query with no close match short-circuits to a canned "couldn't find any indexed code" message without calling the LLM, mirroring Day 18's chat behavior. `EmbeddingConfigError`/`LLMConfigError` map to `503`, `EmbeddingAPIError`/`LLMAPIError`/`VectorStoreError` to `502`, matching every other AI-backed endpoint. 7 new tests (diagnosis with ranked sources, no-hits canned reply with an assertion the LLM is never called, cross-user ownership, both config-error mappings, empty-description validation) — 94/94 backend tests pass. Verified live against the real running app and real database: real user, real GitHub-backed repository, called over HTTP — got the expected `503 OPENAI_API_KEY is not configured` (debugging depends on both provider keys, same as chat, so the embedding gap is hit before the still-open `ANTHROPIC_API_KEY` one), a `422` for an empty description, a `404` for an unknown repository, and confirmed the route's registration via `/openapi.json`. No frontend yet — next UI day follows the same backend-then-UI rhythm as Days 22-25. A real grounded diagnosis remains unverified pending real `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` values. |
| Day 27 | Debug UI — closes the gap Day 26 left open. New `/dashboard/[id]/debug` page, styled after Day 21's search page rather than Day 23/25's file-scoped pages, since debugging is also a repository-wide query with no target file: a plain GET `<form>` round-tripping through the URL's `?description=` param (shareable/bookmarkable, same reasoning Day 21 used), calling Day 26's `debug` endpoint and rendering the diagnosis plus its ranked `sources` (reusing the same result-card layout as search, since `DebugResponse.sources` and `CodeSearchResult` are the same shape). A "Debug" button was added next to Chat/Search on the repository detail page, gated on `status === "completed"` the same way — debugging depends on embeddings existing in Qdrant, unlike explain/review's Day 22 chunk-only independence. `debugRepository()` added to `lib/api.ts`, `next typegen` re-run, `tsc --noEmit` and `eslint` both pass. Verified live in a real Chromium browser via a throwaway Playwright script: a fresh `e2e_`-prefixed user, a real GitHub-backed repository with its status flipped to `completed` directly in the dev database (same technique Days 19/21 used, since real ingestion can't reach `completed` without a real `OPENAI_API_KEY`) — clicked "Debug", submitted a description, and confirmed the graceful inline `OPENAI_API_KEY is not configured` error with the submitted text preserved in the input (a natural side effect of the URL-driven value, same as Day 21). Backend untouched — still 94/94. A real grounded diagnosis with ranked sources (not just the graceful-failure path) remains unverified pending real `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` values. |
| Day 28 | Architecture analysis — `POST /repositories/{id}/architecture`, the "architecture analysis" half of README's last mostly-unstarted planned feature ("Architecture analysis and basic defensive security scanning"); security scanning is left for a later day, same split Days 22/24 made for explain/review. Deliberately independent of embeddings/Qdrant like explain/review, but for a different reason: architecture needs breadth across the whole repository, not top-k similarity to one query, so semantic search doesn't fit here anyway. Instead it grounds the LLM (Day 17) in the repository's full file tree (every `repository_files` row's path and language, sorted) plus the top-level README's reconstructed content (Day 22's `reconstruct_file_content()`) if one exists, and asks it to infer likely components from directory structure, tech stack from file extensions/config files, and entry points from naming conventions — explicitly told these are structural inferences from file organization, not a reading of the source, and not to invent anything the file tree/README don't evidence. A repository with zero scanned files returns `400`; `LLMConfigError`/`LLMAPIError` map to `503`/`502` as usual. New `ArchitectureAnalysisResponse` schema (`analysis`, `file_count`, `readme_path`) so a future UI can show what the analysis was based on. 6 new tests (with/without a README present, cross-user ownership, no-files rejection, config-error mapping) — 100/100 backend tests pass. Verified live against the real running app and real database: real user, real GitHub-backed repository, a directly-inserted README + source file, called over HTTP — got the expected `503 ANTHROPIC_API_KEY is not configured` directly (no `OPENAI_API_KEY` involved at all, confirming the embeddings-independence worked as designed, unlike Day 26's debug which hits the OpenAI gap first), a `404` for an unknown repository, and confirmed the route's registration via `/openapi.json`. No frontend yet, same backend-then-UI rhythm as every prior feature pair. A real analysis from Claude remains unverified pending a real `ANTHROPIC_API_KEY`. |
| Day 29 | Architecture UI — closes the gap Day 28 left open. New `/dashboard/[id]/architecture` page, auto-running the analysis on load like Day 23/25's explain/review pages (no query input, since architecture always analyzes the whole repository) rather than search/debug's URL-param pattern. An "Architecture" button was added to the repository detail page, but — unlike Chat/Search/Debug's `status === "completed"` gate — it's gated on `repository.file_count > 0`, matching Day 28's actual backend requirement (files, not embeddings); this makes it the first top-level action button usable before ingestion reaches a terminal state at all. Caught and fixed the same bug class Day 23 found: the page's metadata line (file count, README path) originally came only from a *successful* response body, so a failed call showed no count either; fixed by sourcing the file count from the repository object already fetched for the page (always available) and reserving the README mention for when the analysis actually succeeded. `analyzeRepositoryArchitecture()` added to `lib/api.ts`, `next typegen` re-run, `tsc --noEmit` and `eslint` both pass. Verified live in a real Chromium browser via a throwaway Playwright script: a real repository with a directly-inserted README + source file and `file_count` set to match (no status flip needed, unlike Days 19/21/27) — confirmed Chat stayed disabled while Architecture was enabled on the same still-`pending` repository, clicked through, and saw "Based on 2 scanned files" plus the graceful inline `ANTHROPIC_API_KEY is not configured` error. Backend untouched — still 100/100. A real analysis with the README mention (not just the graceful-failure path) remains unverified pending a real `ANTHROPIC_API_KEY`. |
| Day 30 | Basic defensive security scanning — `POST /repositories/{id}/security-scan`, the "security scanning" half of README's last planned feature that Day 28 explicitly deferred. Deliberately **not** LLM-backed, unlike every other AI feature in this app: new `app/services/security_scan.py` is a small, fixed set of regex rules for well-known insecure patterns (hardcoded secrets, committed private keys, `eval`/`exec`, shell injection via `os.system`/`shell=True`, insecure `pickle` deserialization, unsafe `yaml.load`, SQL built via string interpolation, unsanitized HTML injection), each tagged with a severity. The endpoint reconstructs every scanned file's content (Day 22's `reconstruct_file_content()`, same independence from embeddings/Qdrant as explain/review/architecture) and runs the rules against each one, returning findings sorted by severity then file/line. Zero files still returns `400`; there's no `LLMConfigError`/`EmbeddingConfigError` handling at all because nothing here calls an external API — this is the first AI-adjacent endpoint in the whole project with **no disclosed gap**. New `SecurityFindingRead`/`SecurityScanResponse` schemas. 17 new tests: 12 unit tests for `scan_content()` directly (one per rule, a clean-code negative case, exact line-number reporting) plus 5 endpoint tests (findings across multiple files, an all-clean repository, cross-user ownership, no-files rejection) — 117/117 backend tests pass. Verified live against the real running app and real database with genuinely unsafe content (not just the graceful-failure path every other Day 22+ feature is stuck on): a real repository with one file containing a hardcoded `DB_PASSWORD` and an `eval()` call plus one clean file got back the exact two findings with correct file paths, line numbers, and severities, and the clean file contributed nothing. That live check caught a real bug before it shipped: the `hardcoded-secret` rule's `\b` anchor doesn't sit between an underscore and the next letter (both are word characters), so prefixed names like `DB_PASSWORD` or `MY_API_KEY` silently didn't match — only bare `password = ...` did. Fixed by loosening the anchor to allow a `\w*` prefix/suffix around the keyword, added a regression test for the exact case, re-verified live. No frontend yet, same backend-then-UI rhythm as every prior feature pair. |
| Day 31 | Security scan UI — closes the gap Day 30 left open. New `/dashboard/[id]/security` page, auto-running the scan on load like Day 23/25/29's file/architecture pages, and a "Security" button on the repository detail page gated on `repository.file_count > 0`, identical to Architecture's gate (both only need scanned files, not embeddings). Each finding renders as a card with `file:line`, a severity badge (`destructive`/`secondary`/`outline` for high/medium/low), the rule's message, and the offending line as a snippet; an all-clean repository shows a plain "no issues found" message rather than an empty list. Because Day 30's endpoint calls no external API, this is the **first UI page in the whole project verified live with real, complete, correct output** rather than a graceful API-key error — every prior verification since Day 23 could only prove the failure path. `scanRepositorySecurity()` added to `lib/api.ts`, `next typegen` re-run, `tsc --noEmit` and `eslint` both pass. Verified live in a real Chromium browser: a real repository with one file containing a hardcoded `DB_PASSWORD` and an `eval()` call plus one clean file — clicked "Security", and both real findings rendered with the correct file, line, severity, and message, while the clean file contributed nothing to the list, exactly matching Day 30's backend behavior. Backend untouched — still 117/117. |
| Day 32 | Specialized AI agent (first slice) — `POST /repositories/{id}/agent`, README's last remaining planned feature ("Specialized AI agents for complex, multi-step tasks"), and the first real dependency change since Day 1: `langgraph==0.6.11` added to `requirements.txt` (pulls in `langchain-core` transitively; no conflicts per `pip check`). Every feature through Day 31 is a fixed pipeline — a request runs one predetermined sequence of steps and returns. This is different: new `app/services/agent.py` builds a small LangGraph `StateGraph` that alternates a "plan" node (asks the LLM, given the goal and a transcript of steps so far, to decide as JSON whether to call a tool or finish) and an "act" node (actually runs the chosen tool), looping until the LLM decides it has enough information or `MAX_TOOL_CALLS` (4) is reached, at which point the next plan call is forced to finish with whatever it has. Two tools for this first slice, both reusing existing services rather than new code paths: `search_code` (Day 14/16's embed-then-search) and `explain_file` (Day 22's reconstruct-then-explain, looked up by exact path this time instead of a file ID). Deliberately still no `langchain-anthropic`/`anthropic` SDK — the plan node and both tools call Day 17's own `generate_response` directly, keeping the same thin-wrapper style as every other service; LangGraph is used purely for the plan/act control flow. A tool failing (e.g. `search_code` hitting the still-open `OPENAI_API_KEY` gap) is caught *inside* the tool and fed back to the agent as an observation it can reason about and adapt to, rather than failing the whole request — only the core planning LLM itself being unavailable maps to `503`/`502`, since without it the agent can't decide anything. `_parse_decision()` tolerates markdown code fences and surrounding prose around the JSON, and falls back to treating unparseable output as a plain-text final answer rather than crashing. New `AgentRequest`/`AgentResponse`/`AgentStepRead` schemas, the response including the full step trace (tool, arguments, summary) for transparency into what the agent actually did. 13 new tests: 5 unit tests for `_parse_decision` (plain JSON, fenced JSON, JSON amid prose, unparseable fallback, unknown-action fallback) plus 8 endpoint tests (immediate finish with no tools, one `search_code` step then finish, one `explain_file` step then finish — exercising the *same* mocked LLM call for both the plan decision and the tool's own internal call, forced-finish after hitting the tool-call cap, ownership, config-error mapping, empty-goal validation) — 130/130 backend tests pass. Verified live against the real running app and real database: real user, real repository, called over HTTP — got the expected `503 ANTHROPIC_API_KEY is not configured` from the very first plan step (confirming the graph actually starts executing rather than failing at import/construction time), correct `422`/`404` handling, and confirmed the route's registration via `/openapi.json`. No frontend yet, same rhythm as every prior feature pair — and this is explicitly a first slice: more tools (review, debug, architecture, security-scan), a higher step budget, and a UI to show the reasoning trace are natural next increments rather than a one-day feature. |
| Day 33 | Agent UI — closes the gap Day 32 left open. New `/dashboard/[id]/agent` page, following Day 27's debug-page pattern (a plain GET `<form>` round-tripping through the URL's `?goal=` param) rather than the auto-running explain/review/architecture/security pattern, since a goal is free-form input the agent needs, not a fixed thing to always check. Renders Day 32's final `answer` plus, distinctively, the full step trace the response already carried — each step as its own card showing the tool name, its arguments (as compact JSON), and its result summary, giving visibility into what the agent actually did rather than just its conclusion (explicitly called out as a goal in Day 32's own entry). An "Agent" button was added to the repository detail page gated on `repository.file_count > 0`, same as Architecture/Security rather than Chat/Search/Debug's `status === "completed"` gate — consistent with the agent's own design, since a tool hitting the embeddings gap is something it already handles gracefully rather than something the whole feature needs to avoid. `runRepositoryAgent()` added to `lib/api.ts`, `next typegen` re-run, `tsc --noEmit` and `eslint` both pass. Verified live in a real Chromium browser via a throwaway Playwright script: a real repository with one directly-inserted file and `file_count` set to match (no status flip needed) — confirmed Chat stayed disabled while Agent was enabled on the same still-`pending` repository, submitted a goal, and got the graceful inline `ANTHROPIC_API_KEY is not configured` error with the goal preserved in the input. Backend untouched — still 130/130. A real multi-step run with a visible step trace (not just the graceful-failure path) remains unverified pending a real `ANTHROPIC_API_KEY`. |
| Day 34 | Celery + Redis background workers — the env contract has said "used from Day 34" since Day 1's `.env.example`, and `repository_ingestion.py` has carried a matching comment since Day 7; this is that promise kept. Ingestion moves off FastAPI's in-process `BackgroundTasks` (Days 7-33) onto a real queue: new `app/core/celery_app.py` (the Celery app, Redis as broker, no result backend since `ingest_repository` already reports outcome via `repositories.status`/`error_message` — a second store for the same information would be redundant) and `app/tasks.py` (one task, wrapping the untouched `ingest_repository` in `asyncio.run()`). `create_repository`/`reindex_repository` swap `background_tasks.add_task(...)` for `ingest_repository_task.delay(str(repository.id))` and drop their now-unused `BackgroundTasks` parameter entirely. New `redis==7.0.1`/`celery==5.6.3` dependencies (`pip check` clean). No Docker in this environment (same constraint as Day 15's Qdrant) and Redis has no official native-Windows build, so — same spirit as Day 15's standalone-binary workaround — Redis runs for real inside this machine's WSL2 Ubuntu instance (`apt install redis-server`; WSL2 forwards `localhost:6379` to Windows automatically once its systemd-managed default of `bind 127.0.0.1` is changed to `bind 0.0.0.0` in `/etc/redis/redis.conf`, otherwise only the WSL VM itself can reach it). Tests changed shape, not count: `conftest.py`'s autouse ingestion stub and the reindex spy test now patch `ingest_repository_task.delay` instead of the old `ingest_repository` import, so the suite still needs neither Redis nor a worker — 130/130 backend tests still pass. Verified live against a **real, separate Celery worker process** (`celery -A app.core.celery_app worker --pool=solo` — `--pool=solo` is required on native Windows, since the default "prefork" pool needs `os.fork()`), not an in-process simulation: created a real repository over HTTP, watched the worker's own log show `Task ingest_repository[...] received` then `succeeded`, and confirmed via the API that `repositories.status` updated to `failed` with a real error message — proving a genuinely separate process consumed the task from Redis and ran the full pipeline. That live run caught a real bug pytest's mocked coverage structurally cannot: a *second* repository created back-to-back crashed the worker with `AttributeError: 'NoneType' object has no attribute 'send'`, because `app.core.database.engine`'s connection pool is a module-level singleton and asyncpg connections are bound to the event loop they were opened on — each Celery task gets a fresh loop from `asyncio.run()`, so the second task inherited a connection tied to the first task's already-closed loop. Fixed by disposing the pool at the end of every task, inside the still-open loop, so the next task's `asyncio.run()` always starts from an empty pool. New regression test (`tests/test_tasks.py`) reproduces the exact failure using a real connection-pooled engine against the test database (unlike `test_repository_ingestion.py`'s existing tests, which monkeypatch `AsyncSessionLocal` to the test's own single already-open session and so never exercise a real pool) — confirmed it fails without the fix and passes with it, then re-verified live: two repositories created back-to-back both completed via the real worker with no crash. 131/131 backend tests pass. README's backend setup section gained a Redis/Celery paragraph alongside the existing Postgres/Qdrant ones. |
| Day 35 | Agent tool expansion — Day 32's own log entry named this a natural next increment: the agent (`app/services/agent.py`) grows from 2 tools to 6, adding `review_file`, `debug`, `architecture`, and `security_scan` alongside the existing `search_code`/`explain_file`, each reusing the same underlying logic as its REST endpoint counterpart (Days 24/26/28/30) rather than a second implementation, just with shorter, tool-appropriate prompts (2-5 sentences instead of a full response) so a multi-step transcript doesn't balloon in size. Two duplicated code paths got factored out in the same pass, both now shared within `agent.py` itself: `_search_chunks()` (embed + Qdrant search + DB join) backs both `search_code` and the new `debug`, and `_load_file_content()` (fetch file + chunks + reconstruct) backs both `explain_file` and the new `review_file`. `security_scan` needed no LLM or embedding call at all, same as its standalone endpoint — the only one of the six tools with zero external dependency. `PLAN_SYSTEM_PROMPT` documents all six tools with their argument shapes (including the two that take none, `architecture()`/`security_scan()`, so the LLM knows to pass `{}`). `MAX_TOOL_CALLS` stays at 4 — a higher step budget was Day 32's other named increment but is explicitly left for a separate day, one change at a time. 4 new endpoint tests (one per new tool, each mocking the plan decision, the tool's own internal LLM/embedding calls where applicable, and the finish decision) — 135/135 backend tests pass. Verified live against the real running app: registered a user, inserted a repository directly (bypassing ingestion entirely, since this day doesn't touch it and the WSL/Redis port-forwarding from Day 34 wasn't active in this session), called the agent with a goal that could plausibly use several of the new tools — got the same `503 ANTHROPIC_API_KEY is not configured` from the plan step as Days 32/33, confirming the graph still builds and executes correctly with the larger tool set and the route registration is unaffected. No frontend changes needed — Day 33's agent UI already renders whatever tools/steps come back generically. |
| Day 36 | Configurable agent step budget — the other increment Days 32 and 35 both explicitly deferred, done properly rather than just bumping the hardcoded constant: `AgentRequest` gains `max_steps` (`Field(default=4, ge=1, le=10)`), threaded through `run_agent()` → `_build_graph()` → `plan_node`'s forced-finish check, replacing the module-level `MAX_TOOL_CALLS` read directly in that function (the constant now only backs `run_agent`'s default parameter, for callers that don't care to pick a number). Reasoning: a goal that plausibly chains several of Day 35's six tools needs more room than a quick one-tool lookup, and one fixed value can't serve both well. Frontend: the Agent page gets a "Max steps" `<select>` (2/4/6/8/10) next to the goal input, following the same URL-param round-trip pattern as the goal itself (`?max_steps=`) rather than component state, so a shared/bookmarked link reproduces the exact same run configuration; `runRepositoryAgent()` gained an optional `maxSteps` argument. 3 new backend tests (custom value respected, both range boundaries rejected with `422`) plus a renamed existing one (`test_agent_forces_finish_after_default_max_steps`, unchanged behavior, just now explicit that it's testing the *default* rather than a hardcoded-forever value) — 137/137 backend tests pass. `tsc --noEmit`/`eslint` clean, `next typegen` re-run. Verified live: registered a user, inserted a repository directly (Redis/WSL from Day 34 wasn't active this session, same recurring quirk noted in Day 35), confirmed over HTTP that `max_steps=11`/`0` both `422` and `max_steps=6` reaches the same `503 ANTHROPIC_API_KEY is not configured` as always; in a real Chromium browser, confirmed the select defaults to 4, changing it to 8 and submitting round-trips through the URL correctly, and the select reflects that value after the page reloads. |
| Day 37 | Permanent e2e coverage for Days 19-36's UI — until today, every page since chat (Day 19) was only ever checked with a throwaway Playwright script written for that day's verification and deleted right after; only `auth.spec.ts` and `repository.spec.ts` (both from Day 13) survived in the actual suite. New `tests-e2e/seed.ts` shells out to the backend's venv Python the same way `global-setup.ts` already does for cleanup, to insert a repository/files/chunks directly and (for the three status-gated pages) flip `status` to `completed` — the same manual techniques Days 19/21/23/27 used by hand, now permanent, and necessary rather than a shortcut: a real `POST /repositories` needs Qdrant/Redis reachable and a real `OPENAI_API_KEY` to ever produce files at all (Day 23's rollback-on-any-failure finding), none of which this environment reliably has. Two new spec files split along the same technical line the app's own UI gating already draws: `file-scoped-ai-features.spec.ts` (explain, review, architecture, security, agent — gated on `file_count > 0`) and `rag-gated-features.spec.ts` (chat, search, debug — gated on `status === "completed"`). Every AI-dependent assertion checks for the graceful inline error rather than a real result, since neither provider key works live in this environment — except security_scan, the one feature needing no external API at all, whose test asserts real, deterministic findings the same way Day 31's live check did. Building these caught two real, unrelated things: (1) a `.match()` against SQL-echo-polluted stdout grabbed the wrong UUID (the owner_id logged earlier in the same INSERT statement, not the repository's own id) — fixed with an unambiguous `SEED_REPOSITORY_ID:` sentinel instead of relying on shape or position; (2) the OpenAI gap's exact wording has silently changed since Days 19/21/27 documented it — a real `OPENAI_API_KEY` now exists in `.env` but its account has zero billing credits, so the actual error is a `429 insufficient_quota` message, not "not configured"; the new tests match either, honestly reflecting that the underlying account state can drift independently of any code here. Confirmed both spec files pass in a real Chromium browser, 8 new tests total. Running the *entire* suite together surfaced a third, pre-existing issue outside Day 37's scope: `repository.spec.ts`'s real add-repository test now hangs past its timeout, because Day 34's `ingest_repository_task.delay(...)` call inside `create_repository`/`reindex_repository` blocks synchronously on Celery's broker-connection retries when Redis isn't reachable (confirmed directly: a bare `redis.Redis(...).ping()` with a 2s connect timeout still didn't return in 15s) — a real architectural gap worth a future day (fail fast or queue asynchronously instead of blocking the request), left undisturbed here since fixing it wasn't this day's goal. |
| Day 38 | Fail-fast repository queueing — closes the real architectural gap Day 37 flagged but explicitly left alone: `create_repository`/`reindex_repository` called `ingest_repository_task.delay(...)` unguarded, and Celery's Redis broker transport can block the HTTP request for a long time (Day 37 measured a bare `redis.Redis(...).ping()` with a 2s connect timeout still not returning after 15s) when Redis is unreachable rather than actively refusing the connection — the TCP SYN just gets no reply, so the OS's own retransmission timeout governs the wait, not any application-level setting. Two-part fix: `app/core/celery_app.py` now sets `broker_connection_timeout` and `broker_transport_options` (`socket_connect_timeout`/`socket_timeout`, both 5s) to bound the connection attempt itself — defense-in-depth for a real deployment, though not guaranteed to be fully honored by every OS/network path (the Day 37 measurement itself suggests as much on this machine). The actual guarantee is the second part: new `_queue_ingestion()` helper in `app/api/repositories.py` wraps every `.delay()` call in a broad `except Exception` (deliberately broad — the broker transport can raise different exception types depending on failure mode: `redis.exceptions.ConnectionError`/`TimeoutError`, `kombu.exceptions.OperationalError`) and, on failure, marks the repository `status: "failed"` with a clear `error_message` instead of leaving it hanging or stuck `pending` forever — reusing the exact same terminal-failure shape ingestion itself already produces, so the frontend's existing failed-state handling covers this for free with no UI changes needed. Both `create_repository` (still `201`) and `reindex_repository` (still `200`) return normally with the failed repository rather than raising `500`/timing out. 2 new tests (create and reindex, each monkeypatching `.delay()` to raise and asserting the terminal `failed` state and message) — 139/139 backend tests pass. Verified live against the real running app and real database with a **genuinely unreachable Redis** (no WSL/Redis active this session, the same recurring environment gap Days 35-37 noted, turned here into the actual test case): `POST /repositories` returned in 10s with `status: "failed"` and `"Could not queue ingestion: the background worker is unreachable"` instead of hanging past Day 37's 15s+ observation; `POST /repositories/{id}/reindex` on the same repository returned in 8s with the same result. Both bounded times land inside the ~2x margin the 5s socket timeouts plus Celery's small internal publish-retry policy would predict, confirming the timeout configuration is actually taking effect end-to-end, not just the fallback exception handling. |
| Day 39 | Closed the loop on Day 37/38: re-ran `repository.spec.ts` against the real app to confirm Day 38 actually fixed the hang. It did — no more timeout — but the test itself still failed twice in a row, for two different, genuine reasons rather than flakiness. First, `getByRole("link", {name: "octocat/Hello-World"})` used Playwright's default 5s assertion timeout, shorter than the now-bounded-but-real ~5-10s `POST /repositories` can take when the broker is unreachable, and the terminal-failure assertion only matched `/OPENAI_API_KEY/`, not Day 38's new "background worker is unreachable" message — fixed both (a 15s timeout on that assertion, and a combined `INGESTION_FAILURE_PATTERN` regex covering all three known terminal messages: unconfigured key, `insufficient_quota`, and the broker-unreachable message), documented inline with the same "match multiple valid outcomes" reasoning Day 37 used for the first two. Second, once the test could get further, it hit a genuine, previously-unreached bug: `DELETE /repositories/{id}` was the one Qdrant-touching endpoint in the whole app that didn't catch `VectorStoreError`, so a real unreachable Qdrant in this session (confirmed directly, same `socket.connect` timeout technique as Day 38's Redis check) surfaced as an unhandled `500` instead of the graceful `502` every other such endpoint (`search`, `debug`) already returns — a straightforward fix (wrap the call, catch, map to `502`) plus a regression test asserting both the status code and that the repository row survives the failed delete (retryable, not orphaned — the exact guarantee the surrounding comment already promised). 1 new backend test — 140/140 pass. Rather than stop at the disclosed-gap pattern the rest of this project uses for missing AI provider keys, found Day 15's standalone `qdrant.exe` and Day 34's WSL `redis-server` still on disk/installed, started both plus a real Celery worker, and re-ran the **entire** frontend e2e suite (not just `repository.spec.ts`) against fully live infrastructure — Redis reachable, a real worker consuming the queue, Qdrant reachable — for the first time in the project's history: all 15 tests passed. Directly confirmed (`generate_embedding` called standalone against the real OpenAI API) that the `.env` key's zero-billing-credits state from Day 37 is unchanged — a real `429 insufficient_quota` — so ingestion still legitimately ends in `failed` rather than `completed`; the value of live infra here isn't a happy-path result but that `create`/`reindex` now genuinely reach the real ingestion pipeline end-to-end (queued, picked up by a real worker, embeddings actually attempted and correctly failed) and `delete` now exercises a real successful Qdrant cleanup, none of which a merely-reachable-vs-unreachable broker/vector-store distinction alone would have exercised. Full backend suite also re-confirmed at 140/140 against this same live infra. `tsc --noEmit`/`eslint` clean. |
| Day 40 | Docker Compose for local infrastructure — the README's tech stack and project structure have named Docker/`docker/` since Day 1, but the directory has sat empty this whole time; every day since 15/34 that needed Postgres/Redis/Qdrant has instead hand-rolled a native install, a standalone `qdrant.exe`, or WSL2 `redis-server`, because this machine's 8 GB of RAM makes Docker Desktop's own overhead a real cost on top of everything else already running. New `docker/docker-compose.yml` (Postgres 17, Redis 7, Qdrant, all `-alpine`/official images) is additive, not a replacement: every port/credential default matches `.env.example` exactly, so it works with zero configuration, and nothing in the backend or its tests references Docker in any way — the app only ever dials `localhost:<port>`, so Compose and the native/WSL setup are two interchangeable ways to make those ports answer, not two different code paths. Deliberately no `HEALTHCHECK` on the `qdrant` service: checked its official Dockerfile directly (its final stage is a minimal `debian-slim`, no guaranteed `bash`/`wget`/`curl`) rather than ship a probe command that assumes tools the image doesn't promise to have — `postgres`/`redis` keep theirs (`pg_isready`/`redis-cli`) since those ship with the images themselves. No `version:` key, matching current Compose V2 (`docker compose`, not the legacy hyphenated CLI) conventions. Named volumes (`repomind_postgres_data`/`_redis_data`/`_qdrant_data`) persist data across restarts. Since this machine genuinely can't run Docker Desktop for real verification (the stated reason for this whole day's approach), the compose file itself was validated the way that's actually available here: parsed with `yaml.safe_load` to confirm it's syntactically valid and structurally matches the Compose Specification, and every image/tool claim above (Postgres/Redis image contents, Qdrant's Dockerfile and lack of shell tools) was checked against source rather than assumed — a live `docker compose up` run itself is a disclosed gap, same shape as every AI-provider-key gap this project already carries, rather than something faked around. What *was* fully live-verified is the actual alternative this file documents: README gained a new "Local infrastructure" section spelling out both options side by side (Docker Compose vs. native Postgres + WSL2 Redis + standalone Qdrant, the latter marked as what this project has actually run on day to day), and Option B's exact instructions were re-run fresh in this session — WSL `redis-server`, the `C:\qdrant-run\qdrant.exe` binary from Day 15, and the existing native Postgres install — confirming all three reachable and the full 140-test backend suite green against them. The three previously-duplicated inline Postgres/Redis/Qdrant setup blurbs in the Backend section were consolidated to point at the new section instead of repeating (and, in Postgres's case, contradicting — "there is no Docker Compose service for it yet" was now simply false) themselves. |
| Day 41 | Delete's error handling gap — `deleteRepositoryAndRedirect` (`src/app/actions/repositories.ts`) had none at all: unlike `reindexRepositoryAction`, which already fails gracefully, a failed `DELETE` threw unhandled and crashed the whole page (first seen live in Day 39's investigation, not fixed there since it was outside that day's scope). Fixed by catching the failure and redirecting back to the same detail page with `?deleteError=<message>` instead of throwing — chose "stay on the page and show why" over reindex's silent-swallow-and-revalidate, since delete has no status badge to fall back on for communicating what happened. `RepositoryDetailPage` reads the new `deleteError` search param and renders it in the same destructive-alert style already used for a `failed` repository's `error_message`. New Playwright test seeds a repository and navigates directly to `?deleteError=...` to deterministically exercise the new rendering branch (a real backend failure isn't something a test can force on demand) — `tsc --noEmit`/`eslint` clean, and this new test plus the 3 pre-existing `repository.spec.ts` tests all pass. Re-running the full spec caught the fix actually working end-to-end by accident: with Qdrant stopped after Day 40's verification, the pre-existing "add, view, reindex, and delete" test's real `DELETE` call failed with a genuine `500` (not Day 39's `502` — see below) and the page showed the graceful message instead of crashing, proving the fix handles a real, not just synthetic, failure. That `500` instead of `502` is itself a real, distinct finding: `vector_store.py`'s functions only ever check `response.status_code` — a connection-level failure (Qdrant fully unreachable, not just returning an error status) raises a raw `httpx.ConnectError` that isn't caught anywhere, so it never becomes a `VectorStoreError` and Day 39's `except VectorStoreError` catch in `delete_repository` never fires. Checked the sibling services (`embeddings.py`, `github.py`, `llm.py`) and none of them catch connection-level httpx errors either — this is a systemic gap across every external-API wrapper in the backend, not something specific to delete or to Qdrant. Deliberately left unfixed today: it's real, but touches four files uniformly rather than one, and fixing it properly (wrapping connection-level errors into each service's existing typed exception, the same shape Day 39 used) is its own well-scoped day, not a rider on this one. Restarted Qdrant/Redis/a real Celery worker to confirm the pre-existing test passes normally with live infra (it does, all 4 tests green) — the `500` was purely this session's infra state, not a regression. Full backend suite reconfirmed at 140/140 against the same live infra. |
| Day 42 | Closed the systemic gap Day 41 flagged: `vector_store.py`, `embeddings.py`, `github.py`, and `llm.py` each wrap their one-or-few `httpx` calls in `try`/`except httpx.RequestError`, converting a connection-level failure (unreachable host, connection refused, DNS failure, timeout - anything raised *before* a response exists to check `.status_code` on) into that module's own existing typed error (`VectorStoreError`/`EmbeddingAPIError`/`GitHubAPIError`/`LLMAPIError`), the same exception every caller already catches for a bad HTTP status. No new exception types, no new caller-side handling anywhere - every `except VectorStoreError`/etc. across `app/api/repositories.py` and `app/services/agent.py` now correctly catches this failure mode too, for free. `vector_store.py` needed this at all four of its call sites (`ensure_collection`'s two calls share one `try`, `upsert_chunks`, `search`, `delete_by_repository` each their own) since it's the only one of the four with more than a single external call. 10 new regression tests (4 in `test_vector_store.py`, one per function, mirroring that file's existing per-function `raises_on_api_error` granularity; one each in `test_embeddings.py`/`test_llm.py`; a new `tests/test_github.py` - `fetch_repository` had no dedicated unit tests at all before today, only indirect coverage via `test_repositories.py`'s endpoint-level mocking - with a minimal success/404/error baseline plus the connection-failure case), each simulating the failure by raising `httpx.ConnectError`/`ConnectTimeout` from inside the `httpx.MockTransport` handler rather than returning a `Response`, the same mocking technique every one of these files already used, just exercising a different failure mode. 150/150 backend tests pass, no live network or API keys needed for any of them. Verified live three ways: (1) the unit tests themselves, (2) a direct HTTP call against the real running app with a real user/repository and Qdrant genuinely stopped - `DELETE /repositories/{id}` now returns `502 {"detail":"Could not reach Qdrant: All connection attempts failed"}` instead of Day 41's raw unhandled `500`, and (3) re-ran `repository.spec.ts` in a real browser with the same genuinely-stopped Qdrant - the pre-existing "add, view, reindex, and delete" test still fails at its delete-success assertion (that part still needs live Qdrant, unrelated to today), but now redirects with the exact new clean message (`?deleteError=Could%20not%20reach%20Qdrant...`) visible in the real UI instead of crashing; the other 3 tests in that file, including Day 41's own graceful-error test, all pass. Scope was deliberately held to the four named files - `repository_ingestion.py` makes its own separate `httpx` call (the tarball download) with the same unaddressed gap, noted here rather than folded in, since it wasn't part of what this day was scoped to touch. |

